---
import { Image } from "astro:assets";
import heroImage from "@assets/banner.jpg";

interface Props {
  id?: string;
}

const { id = "inicio" }: Props = Astro.props;
---

<section id={id} class="relative isolate overflow-hidden text-white">
  <div class="absolute inset-0">
    <Image
      src={heroImage}
      alt="Cuenco tibetano sobre un tapete"
      class="h-full w-full object-cover"
      widths={[640, 960, 1280, 1600]}
      sizes="100vw"
      loading="eager"
      decoding="async"
    />
  </div>

  <div
    class="relative container mx-auto flex min-h-[75vh] w-full flex-col items-center justify-center gap-6 px-6 py-28 text-center"
  >
    <h1
      class="font-heading text-4xl font-medium italic leading-none tracking-[0.02em] text-white md:text-5xl"
    >
      MANIFIESTA TU SUEÑO CON LA MAGIA DEL SOUND HEALING
    </h1>
    <p class="max-w-3xl font-sans text-2xl font-normal leading-none text-white">
      Si este curso llegó a ti, no es casualidad. No hay coincidencias, solamente
      diosidencias.
    </p>
  </div>
</section>

<style>
  @reference "../styles/global.css";
</style>
